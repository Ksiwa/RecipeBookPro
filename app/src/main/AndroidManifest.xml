<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required for Firebase Authentication and Firestore network access. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecipeBookPro">

        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.RecipeBookPro.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.RecipeBookPro.NoActionBar" />

        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.RecipeBookPro.NoActionBar" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.RecipeBookPro.NoActionBar" />

        <activity
            android:name=".ui.recipe.AddRecipeActivity"
            android:exported="false"
            android:theme="@style/Theme.RecipeBookPro.NoActionBar"
            android:windowSoftInputMode="adjustResize|stateHidden" />

        <activity
            android:name=".ui.recipe.RecipeDetailActivity"
            android:exported="true"
            android:theme="@style/Theme.RecipeBookPro.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="recipebook" android:host="recipe" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.book.BookReaderActivity"
            android:exported="false"
            android:theme="@style/Theme.RecipeBookPro.NoActionBar" />

        <activity
            android:name=".ui.cooking.CookingModeActivity"
            android:exported="false"
            android:theme="@style/Theme.RecipeBookPro.NoActionBar"
            android:screenOrientation="portrait"
            android:keepScreenOn="true" />

        <activity
            android:name=".ui.recipe.RecipeAddEditActivity"
            android:exported="false"
            android:theme="@style/Theme.RecipeBookPro.NoActionBar"
            android:windowSoftInputMode="adjustResize|stateHidden" />

    </application>

</manifest>
